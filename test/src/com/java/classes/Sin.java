package com.java.classes;

public class Sin {

	int angle = 0;
	
	public double sin() {
		return Math.sin(angle);
	}
	public static double sin(int angle) {
		return Math.sin(angle);
	}
	
}
